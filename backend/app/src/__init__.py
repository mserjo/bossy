# backend/app/src/__init__.py
# -*- coding: utf-8 -*-
"""
Пакет `src` є основним контейнером для вихідного коду програми Kudos.

Цей файл `__init__.py` позначає директорію `src` як пакет Python, що дозволяє
структурувати та імпортувати модулі, які знаходяться всередині. Він є
точкою входу для доступу до підпорядкованих пакетів та модулів, таких як:
- `api`: Визначення ендпоінтів FastAPI та обробників запитів.
- `core`: Фундаментальні компоненти програми (базові класи, константи, винятки, залежності).
- `config`: Конфігураційні налаштування (загальні, база даних, логування, Redis, безпека).
- `models`: Моделі даних SQLAlchemy.
- `schemas`: Схеми Pydantic для валідації даних та серіалізації.
- `services`: Шар бізнес-логіки.
- `repositories`: Шар доступу до даних (робота з базою даних).
- `utils`: Допоміжні утиліти загального призначення.
- `tasks`: Фонові завдання Celery (якщо використовуються).
- `locales`: Файли локалізації (якщо використовуються).

Зазвичай цей файл може бути порожнім або містити імпорти для зручного
доступу до ключових компонентів з підпорядкованих модулів.
"""

# Приклади можливих ре-експортів (розкоментуйте та адаптуйте за потреби):

# from .config import settings
# from .main import app # Якщо main.py знаходиться тут і визначає об'єкт app

# __all__ = [
#     "settings",
#     "app",
# ]

# Ініціалізація логера для цього пакета (опціонально, якщо потрібне логування на рівні src)
# from .config.logging import get_logger # Потрібно бути обережним з порядком імпорту
# logger = get_logger(__name__)
# logger.info("Пакет 'src' ініціалізовано.")
