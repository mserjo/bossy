# backend/app/src/api/__init__.py
# -*- coding: utf-8 -*-
"""
Ініціалізаційний файл для пакету 'api'.

Цей пакет відповідає за всю логіку, пов'язану з API ендпоінтами додатку.
Він включає:
- Головний роутер API, який агрегує роутери різних версій (передбачається у `router.py`).
- Підпакети для кожної версії API (наприклад, `v1/`).
- Підпакет для GraphQL API (`graphql/`).
- Підпакет для API, призначених для взаємодії з зовнішніми системами (вебхуки) (`external/`).
- Модулі для специфічних залежностей API (`dependencies.py`), middleware (`middleware.py`)
  та обробки винятків (`exceptions.py`), якщо такі потрібні на цьому рівні абстракції.

Цей файл робить каталог 'api' пакетом Python, що дозволяє імпортувати модулі,
які знаходяться всередині цього каталогу.

Основні компоненти, що очікуються в цьому пакеті:
- `router.py`: Головний агрегатор API роутерів.
- `dependencies.py`: Залежності, специфічні для API (наприклад, перевірка API ключів).
- `middleware.py`: Middleware, специфічне для API.
- `exceptions.py`: Обробники винятків, специфічні для API.
- `v1/`: Підпакет для першої версії REST API.
- `graphql/`: Підпакет для реалізації GraphQL API.
- `external/`: Підпакет для ендпоінтів, що приймають вебхуки від зовнішніх сервісів.
"""

# TODO: Розглянути можливість імпорту та експорту головного API роутера,
# коли він буде створений в `backend.app.src.api.router.py`
# для спрощення імпортів в `backend.app.main.py`.
# from .router import router as main_api_router
#
# __all__ = [
#     "main_api_router",
# ]

# TODO: Додати будь-який інший необхідний код ініціалізації для пакета 'api',
# наприклад, конфігурацію специфічних для API налаштувань або сервісів,
# якщо це не буде зроблено в `app.main` або спеціалізованих конфігураційних файлах.
