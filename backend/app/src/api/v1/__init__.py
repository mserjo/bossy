# backend/app/src/api/v1/__init__.py
# -*- coding: utf-8 -*-
"""
Ініціалізаційний файл для пакету 'v1' API.

Цей пакет містить всю логіку, пов'язану з першою версією (v1) REST API додатку.
Він включає:
- Головний роутер для API v1 (очікується у `router.py`), який агрегує роутери
  для окремих доменних сутностей (користувачі, групи, завдання тощо).
- Підпакети для кожної групи ендпоінтів, що відповідають певним
  ресурсам системи (наприклад, `auth/`, `users/`, `groups/`, `tasks/`).

Цей файл робить каталог 'v1' пакетом Python.
Його основна мета - експортувати головний роутер API v1
(з `backend.app.src.api.v1.router.py`) для використання
в головному API роутері (`backend.app.src.api.router.py`).
"""

# Імпорт та експорт головного роутера для API версії 1.
# Це дозволяє іншим модулям імпортувати роутер v1 безпосередньо з цього пакету:
# from backend.app.src.api.v1 import v1_router
# TODO: Розкоментувати, коли backend.app.src.api.v1.router буде створено.
# На даний момент, імпорт закоментовано, щоб уникнути помилок, якщо файл ще не існує.
# from backend.app.src.api.v1.router import router as v1_router

# __all__ = [
# "v1_router",
# ]

# TODO: Коли `backend.app.src.api.v1.router.py` буде створений та міститиме
# змінну `router`, розкоментуйте наведені вище рядки для коректного експорту.

# TODO: Додати будь-який інший необхідний код ініціалізації для пакета 'v1',
# якщо це буде потрібно (наприклад, конфігурація специфічних для v1 API
# налаштувань або сервісів), хоча зазвичай це робиться на вищих рівнях
# або в спеціалізованих конфігураційних файлах.
