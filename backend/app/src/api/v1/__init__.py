# backend/app/src/api/v1/__init__.py
# -*- coding: utf-8 -*-
"""
Пакет для першої версії (v1) API системи Kudos.

Цей пакет містить усі компоненти, специфічні для версії v1 API,
включаючи головний роутер для цієї версії (`v1_router`), який агрегує
ендпоінти для різних модулів системи (користувачі, групи, завдання тощо).

Основні компоненти:
- `router.py`: Визначає `v1_router` та підключає до нього роутери окремих модулів (наприклад, system, auth).
- `system/`: Підпакет для системних ендпоінтів v1 (налаштування, моніторинг, здоров'я системи).
- `auth/`: Підпакет для ендпоінтів автентифікації та авторизації v1.
- (інші підпакети для груп, завдань, бонусів тощо)

Головний роутер `v1_router` експортується для підключення до загального `api_router`
в `app.src.api.router`.
"""

import logging

# Головний роутер для v1 API буде визначено в .router та імпортовано тут для можливого ре-експорту.
from .router import v1_router

__all__ = [
    "v1_router",
]

logger = logging.getLogger(__name__)
logger.info("Пакет 'api.v1' ініціалізовано.")
