# backend/app/src/services/__init__.py
# -*- coding: utf-8 -*-
"""
Пакет сервісів (`services`).

Цей пакет містить класи сервісів, які інкапсулюють бізнес-логіку додатку.
Сервіси виступають як проміжний шар між API (контролерами/ендпоінтами) та
шаром доступу до даних (репозиторіями).

Кожен підпакет (наприклад, `auth`, `groups`, `tasks`) містить сервіси,
що стосуються відповідних доменних областей.

Основні принципи:
- Сервіси оперують Pydantic схемами для вхідних та вихідних даних.
- Сервіси використовують репозиторії для взаємодії з базою даних.
- Сервіси реалізують бізнес-правила, валідацію (додаткову до схем),
  оркестрацію викликів до інших сервісів або утиліт.
- Методи сервісів є асинхронними.
- Сервіси відповідають за обробку специфічних для бізнес-логіки винятків
  та можуть використовувати кастомні винятки з `core.exceptions`.
"""

# Імпорт базового сервісу (якщо він буде створений)
# from .base import BaseService # Приклад

# TODO: Можна додати експорт конкретних сервісів сюди для зручності,
# якщо вони будуть часто використовуватися з інших частин програми.
# Наприклад:
# from .auth.user_service import user_service # Приклад, коли сервіс буде створено
# from .auth.auth_service import auth_service # Приклад

__all__ = [
    # "BaseService", # Приклад
    # "user_service",  # Приклад
    # "auth_service",  # Приклад
]

from backend.app.src.config.logging import logger
logger.debug("Пакет 'services' ініціалізовано.")

# TODO: Забезпечити правильну структуру підпакетів (`auth`, `groups` тощо)
# всередині `services/` відповідно до `structure-claude-v3.md`.
