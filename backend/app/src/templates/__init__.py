# backend/app/src/templates/__init__.py
# -*- coding: utf-8 -*-
"""
Ініціалізаційний файл для пакету 'templates'.

Цей пакет призначений для зберігання шаблонів, які використовуються
в backend додатку. В основному, це стосується шаблонів для:
- Email повідомлень (HTML або текстові).
- SMS повідомлень (текстові).
- Можливо, інших типів повідомлень або документів, що генеруються на основі шаблонів.

Структура підкаталогів:
- `email/`: Містить HTML та/або текстові шаблони для email.
- `sms/`: Містить текстові шаблони для SMS.

Цей файл робить каталог 'templates' пакетом Python.
Він зазвичай не містить коду, а лише слугує для організації шаблонів.
Доступ до шаблонів зазвичай реалізується через конфігурацію шаблонізатора
(наприклад, Jinja2), який вказує на цей каталог як на джерело шаблонів.
"""

# Цей файл зазвичай залишається порожнім.
# Логіка завантаження та рендерингу шаблонів знаходиться в сервісах
# (наприклад, в сервісі відправки email).

# from jinja2 import Environment, FileSystemLoader
# import os

# TEMPLATE_DIR = os.path.dirname(os.path.abspath(__file__))

# # Ініціалізація Jinja2 Environment, якщо це робиться централізовано тут.
# # Але частіше це робиться в конфігураційному файлі або сервісі.
# template_env = Environment(
#     loader=FileSystemLoader(TEMPLATE_DIR),
#     autoescape=True # Залежить від типу шаблонів (наприклад, для HTML)
# )

# def render_template(template_name: str, **context) -> str:
#     """
#     Допоміжна функція для рендерингу шаблону.
#     (Може бути винесена в окремий утилітарний модуль).
#     """
#     return template_env.get_template(template_name).render(**context)

# __all__ = [
#     # "template_env", # Якщо експортується середовище Jinja2
#     # "render_template", # Якщо експортується функція рендерингу
# ]

pass
