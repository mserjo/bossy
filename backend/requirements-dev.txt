# backend/requirements-dev.txt
# # Цей файл містить перелік залежностей, які необхідні виключно
# # для розробки, тестування, лінтингу та форматування коду.
# # Ці пакети не повинні встановлюватися в продуктивному середовищі,
# # щоб зменшити розмір кінцевого образу та кількість потенційних вразливостей.
# #
# # Рекомендується фіксувати версії для всіх залежностей для забезпечення
# # стабільності та відтворюваності середовища розробки.
# # Встановлюються разом з `requirements.txt` у середовищі розробки.

# --- Тестування ---
pytest>=7.0.0,<8.3.0          # Фреймворк для написання та запуску тестів
pytest-cov>=4.0.0,<5.1.0      # Плагін для pytest для вимірювання покриття коду тестами
pytest-asyncio>=0.20.0,<0.24.0 # Плагін для pytest для підтримки асинхронних тестів (з використанням async/await)
httpx>=0.24.0,<0.28.0         # Клієнт HTTP, необхідний для роботи TestClient в FastAPI

# --- Лінтери та форматувальники коду ---
ruff>=0.0.200,<0.5.0         # Надзвичайно швидкий лінтер Python, написаний на Rust (може замінити flake8, isort, pydocstyle тощо)
black>=23.0.0,<25.0.0         # Безкомпромісний форматувальник коду Python ("The Uncompromising Code Formatter")
isort>=5.10.0,<5.14.0         # Утиліта для автоматичного сортування імпортів у Python файлах
mypy>=1.0.0,<1.11.0           # Статичний аналізатор типів для Python
# flake8>=6.0.0,<7.1.0        # Традиційний лінтер для перевірки стилю коду та помилок (може бути повністю замінений Ruff)

# --- Інструменти для розробки ---
# ipython>=8.0.0,<8.26.0        # Покращена інтерактивна оболонка Python, зручна для експериментів та налагодження
# pre-commit>=3.0.0,<3.8.0    # Фреймворк для управління та підтримки pre-commit хуків (для автоматичних перевірок перед комітом)
# factory-boy>=3.2.0,<3.4.0   # Бібліотека для створення тестових даних (фікстур) для моделей або об'єктів
# coverage>=7.0.0,<7.6.0      # Інструмент для вимірювання покриття коду тестами (якщо не використовується pytest-cov)

# --- Інструменти для документування (якщо проект використовує Sphinx або MkDocs) ---
# sphinx>=7.0.0,<7.4.0          # Потужний генератор документації
# mkdocs>=1.5.0,<1.7.0          # Простий та швидкий генератор статичних сайтів для документації
# mkdocs-material>=9.0.0,<9.6.0  # Популярна тема для MkDocs, що надає сучасний вигляд та функціонал

# --- Інше (специфічні інструменти для розробки) ---
# watchfiles>=0.18.0,<0.23.0  # Бібліотека для відстеження змін у файлах, може використовуватися для live reload,
                             # якщо uvicorn[standard] не включає його або потрібна специфічна версія/поведінка.
