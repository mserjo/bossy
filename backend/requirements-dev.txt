# backend/requirements-dev.txt
# # Залежності, необхідні лише для розробки та тестування.
# # Ці пакети не будуть встановлені в продуктивному образі.
# # Рекомендується також фіксувати версії.

# --- Тестування ---
pytest>=7.0.0,<8.3.0          # Фреймворк для тестування
pytest-cov>=4.0.0,<5.1.0      # Плагін для pytest для вимірювання покриття коду тестами
pytest-asyncio>=0.20.0,<0.24.0 # Плагін для pytest для підтримки асинхронних тестів (async/await)
# httpx>=0.24.0,<0.28.0       # Потрібен для TestClient в FastAPI, якщо ще не в основних requirements.txt

# --- Лінтери та форматувальники ---
ruff>=0.0.200,<0.5.0         # Дуже швидкий лінтер Python, написаний на Rust (може замінити flake8, isort, etc.)
black>=23.0.0,<25.0.0         # Безкомпромісний форматувальник коду Python
isort>=5.10.0,<5.14.0         # Утиліта для сортування імпортів
# flake8>=6.0.0,<7.1.0        # Лінтер для перевірки стилю коду (може бути замінений Ruff)
# mypy>=1.0.0,<1.11.0           # Статичний аналізатор типів

# --- Інструменти для розробки ---
# ipython>=8.0.0,<8.26.0        # Покращена інтерактивна оболонка Python
# pre-commit>=3.0.0,<3.8.0    # Фреймворк для управління pre-commit хуками
# factory-boy>=3.2.0,<3.4.0   # Для створення тестових даних (фікстур)
# coverage>=7.0.0,<7.6.0      # Для вимірювання покриття коду (якщо не використовується pytest-cov)

# --- Для документування (якщо використовується Sphinx або MkDocs) ---
# sphinx>=7.0.0,<7.4.0
# mkdocs>=1.5.0,<1.7.0
# mkdocs-material>=9.0.0,<9.6.0

# --- Інше ---
# watchfiles>=0.18.0,<0.23.0  # Для live reload, якщо uvicorn[standard] не включає його або потрібна специфічна версія
