# backend/requirements.txt
# Цей файл містить список основних Python залежностей, необхідних для роботи
# проекту в продуктивному середовищі. Кожна залежність повинна мати
# чітко вказану версію або діапазон версій для забезпечення стабільності та
# відтворюваності збірок. Залежності, потрібні лише для розробки або
# тестування, слід додавати до файлу `requirements-dev.txt`.

# --- FastAPI та ASGI сервер ---
# FastAPI - сучасний веб-фреймворк для створення API.
# Uvicorn - ASGI сервер для запуску FastAPI додатків.
fastapi  #>=0.109.0,<0.112.0   # Рекомендовано використовувати останні стабільні версії.
uvicorn[standard]  #>=0.27.0,<0.30.0 # Опція [standard] включає продуктивні залежності, такі як uvloop.

# --- Pydantic (Версія 2) ---
# Pydantic використовується для валідації даних та управління налаштуваннями.
pydantic[email]  #>=2.5.0,<2.8.0
pydantic-settings  #>=2.1.0,<2.4.0 # Для керування конфігурацією додатку через змінні середовища.

# --- База даних та ORM (SQLAlchemy Версія 2) ---
# SQLAlchemy - потужна ORM для роботи з базами даних.
# Asyncpg - асинхронний драйвер для PostgreSQL, необхідний для SQLAlchemy в асинхронному режимі.
# Alembic - інструмент для управління міграціями схеми бази даних.
sqlalchemy  #>=2.0.20,<2.1.0 # Забезпечує використання можливостей SQLAlchemy 2.x.
asyncpg  #>=0.28.0,<0.30.0
alembic  #>=1.12.0,<1.14.0

# --- Автентифікація та Авторизація ---
# python-jose - для роботи з JWT (JSON Web Tokens), включаючи створення та перевірку.
# passlib - для хешування та перевірки паролів. Опція [bcrypt] додає підтримку bcrypt.
python-jose[cryptography]  #>=3.3.0,<3.4.0
passlib[bcrypt]  #>=1.7.4,<1.8.0

# --- Логування ---
# estructlog - для структуруваного логування, особливо корисний у контейнеризованих середовищах.
# loguru - альтернативний варіант для більш зручного логування.
# Оберіть один або залиште закоментованим, якщо стандартного логування достатньо.
# structlog>=23.0.0,<25.0.0
# loguru>=0.7.0,<0.8.0
python-json-logger

# --- HTTP клієнт (опціонально) ---
# HTTPX - сучасний асинхронний HTTP клієнт, якщо ваш додаток взаємодіє з іншими API.
# httpx>=0.25.0,<0.28.0

# --- Фонові задачі (Celery, опціонально) ---
# Celery - для виконання фонових завдань асинхронно.
# Redis - часто використовується як брокер повідомлень для Celery.
celery  # >=5.3.0,<5.4.0
redis  # >=5.0.0,<5.1.0 # Клієнт для Redis

# --- Інструменти для роботи з електронною поштою (опціонально) ---
# python-multipart - необхідний FastAPI для обробки форм (`multipart/form-data`), включаючи завантаження файлів.
python-multipart  #>=0.0.7,<0.0.10 # Часто потрібен для форм, навіть якщо не для email.
# emails - для зручного створення та надсилання електронних листів.
# email-validator - для валідації адрес електронної пошти з Pydantic.
# emails>=0.6,<0.7
# email-validator>=2.0.0,<2.2.0 # Використовується Pydantic для типів EmailStr

# --- Інше (опціонально) ---
# python-dotenv - для завантаження змінних середовища з .env файлів.
# Може бути непотрібним, якщо pydantic-settings повністю керує завантаженням .env.
# python-dotenv>=1.0.0,<1.2.0

# Для інтернаціоналізації та локалізації (i18n/l10n).
# Babel>=2.13.0,<2.15.0

# Для розширеної роботи з датами та часом.
# python-dateutil>=2.8.0,<2.10.0

# Для роботи з форматами файлів, наприклад, CSV, Excel (якщо потрібно).
# openpyxl>=3.1.0,<3.2.0 # Для роботи з Excel файлами (.xlsx)
# python-csv>=0.1.0 # Приклад, зазвичай вбудований модуль csv використовується.

# Переконайтеся, що версії сумісні з Python 3.13 та між собою.
# Регулярно оновлюйте залежності та тестуйте додаток після оновлень.
