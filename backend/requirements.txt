# backend/requirements.txt
# # Цей файл містить перелік основних залежностей, необхідних для роботи
# # додатку в продуктивному середовищі. Кожна залежність повинна мати
# # чітко вказану версію або діапазон версій для забезпечення стабільності
# # та відтворюваності збірки.
# #
# # Для залежностей, що використовуються лише під час розробки або тестування,
# # будь ласка, використовуйте файл `requirements-dev.txt`.
# #
# # Рекомендується регулярно оновлювати версії залежностей до останніх
# # стабільних версій, попередньо протестувавши їх сумісність.

# --- FastAPI та ASGI сервер ---
fastapi>=0.100.0,<0.112.0 # Сучасна версія FastAPI, що забезпечує високу продуктивність
uvicorn[standard]>=0.22.0,<0.30.0 # ASGI сервер для FastAPI; [standard] включає оптимізовані залежності (наприклад, на основі Cython) для кращої продуктивності

# --- Pydantic (Валідація даних та налаштування) ---
# Pydantic V2 використовується для строгої типізації та валідації
pydantic>=2.0.0,<3.0.0 # Основна бібліотека для валідації даних
pydantic-settings>=2.0.0,<3.0.0 # Для керування налаштуваннями додатку через змінні середовища та .env файли

# --- База даних та ORM (SQLAlchemy) ---
sqlalchemy>=2.0.0,<2.1.0 # SQLAlchemy 2.0 для асинхронної роботи з базою даних
asyncpg>=0.27.0,<0.30.0 # Асинхронний драйвер для PostgreSQL, необхідний для SQLAlchemy в асинхронному режимі
alembic>=1.10.0,<1.14.0 # Інструмент для міграцій схеми бази даних SQLAlchemy

# --- Автентифікація та Авторизація (JWT та хешування паролів) ---
python-jose[cryptography]>=3.3.0,<3.4.0 # Для створення, перевірки та декодування JWT токенів; [cryptography] додає підтримку асиметричних алгоритмів
passlib[bcrypt]>=1.7.4,<1.8.0 # Для безпечного хешування та перевірки паролів (використовує bcrypt)

# --- Опціонально: HTTP клієнт (для взаємодії з іншими сервісами) ---
# httpx>=0.24.0,<0.28.0 # Асинхронний HTTP клієнт, якщо потрібно надсилати запити до інших API або сервісів

# --- Опціонально: Фонові задачі (Celery) ---
# celery>=5.3.0,<5.4.0 # Розподілена черга завдань для виконання фонових операцій
# redis>=4.6.0,<5.1.0 # Клієнт для Redis, часто використовується як брокер повідомлень та бекенд результатів для Celery

# --- Опціонально: Інше ---
# python-dotenv>=0.21.0,<1.2.0 # Для завантаження змінних середовища з .env файлу (pydantic-settings вже надає цю функціональність)
# email-validator>=2.0.0,<2.2.0 # Для розширеної валідації email-адрес; використовується Pydantic, якщо встановлено

# --- Опціонально: Локалізація та Інтернаціоналізація (i18n/l10n) ---
# babel>=2.12.0,<2.15.0 # Бібліотека для форматування дат, чисел, перекладу текстів тощо, якщо потрібна підтримка кількох мов

# --- Опціонально: Робота з датами та часом ---
# python-dateutil>=2.8.0,<2.10.0 # Розширені можливості для парсингу та маніпуляції датами/часом, якщо стандартних недостатньо
